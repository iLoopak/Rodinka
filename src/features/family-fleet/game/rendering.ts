import type { FleetState } from './core'
export function renderFleet(ctx:CanvasRenderingContext2D,s:FleetState,accent:string,reduced=false){ctx.clearRect(0,0,s.w,s.h); const g=ctx.createLinearGradient(0,0,0,s.h); g.addColorStop(0,'#11182f');g.addColorStop(1,'#050710');ctx.fillStyle=g;ctx.fillRect(0,0,s.w,s.h); ctx.fillStyle='#fff'; for(let i=0;i<50;i++){const x=(i*73+s.time*12)%s.w,y=(i*137+s.time*(12+i%3*15))%s.h; ctx.globalAlpha=.25+(i%4)*.15; ctx.fillRect(x,y,2,2)} ctx.globalAlpha=1; for(const e of s.entities){if(e.kind==='bullet'){ctx.fillStyle='#b9f7ff';ctx.shadowColor='#7cecff';ctx.shadowBlur=10;ctx.beginPath();ctx.roundRect(e.x-3,e.y-10,6,18,4);ctx.fill();ctx.shadowBlur=0}else if(e.kind==='star'){ctx.fillStyle='#f2c85b';ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,Math.PI*2);ctx.fill()}else if(e.kind==='power'){ctx.strokeStyle=e.type==='shield'?'#8DB9C7':e.type==='twin'?'#E9785E':'#8BC6AD';ctx.lineWidth=4;ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#fff';ctx.fillText(e.type==='shield'?'◌':e.type==='twin'?'Ⅱ':'✦',e.x-5,e.y+5)}else{ctx.fillStyle=e.type==='comet'?'#7d6f66':e.type==='scout'?'#cc859f':e.type==='drifter'?'#a89bcb':'#9b8a77';ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#fff5';ctx.stroke()}} const p=s.player; if(p.shield>0){ctx.strokeStyle='#8DB9C7';ctx.lineWidth=3;ctx.beginPath();ctx.arc(p.x,p.y,30,0,Math.PI*2);ctx.stroke()} if(p.inv>0&&!reduced)ctx.globalAlpha=.55+.35*Math.sin(s.time*24); ctx.fillStyle=accent;ctx.beginPath();ctx.moveTo(p.x,p.y-28);ctx.lineTo(p.x+24,p.y+24);ctx.lineTo(p.x,p.y+12);ctx.lineTo(p.x-24,p.y+24);ctx.closePath();ctx.fill();ctx.fillStyle='#fff';ctx.fillRect(p.x-5,p.y-6,10,18);ctx.globalAlpha=1}
