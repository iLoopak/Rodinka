import type { FleetState } from './core'
import { DEFAULT_LOADOUT, type FleetLoadout } from '../cosmetics'
import { drawCabin, drawEngineTrail, drawHull, drawImpact, drawWings } from './cosmeticsRendering'
const IMPACT_DURATION = 0.6
export function renderFleet(ctx:CanvasRenderingContext2D,s:FleetState,accent:string,reduced=false,loadout:FleetLoadout=DEFAULT_LOADOUT){ctx.clearRect(0,0,s.w,s.h); const g=ctx.createLinearGradient(0,0,0,s.h); g.addColorStop(0,'#11182f');g.addColorStop(1,'#050710');ctx.fillStyle=g;ctx.fillRect(0,0,s.w,s.h); ctx.fillStyle='#fff'; for(let i=0;i<50;i++){const x=(i*73+s.time*12)%s.w,y=(i*137+s.time*(12+i%3*15))%s.h; ctx.globalAlpha=.25+(i%4)*.15; ctx.fillRect(x,y,2,2)} ctx.globalAlpha=1; for(const e of s.entities){if(e.kind==='bullet'){ctx.fillStyle='#b9f7ff';ctx.shadowColor='#7cecff';ctx.shadowBlur=10;ctx.beginPath();ctx.roundRect(e.x-3,e.y-10,6,18,4);ctx.fill();ctx.shadowBlur=0}else if(e.kind==='star'){ctx.fillStyle='#f2c85b';ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,Math.PI*2);ctx.fill()}else if(e.kind==='power'){ctx.strokeStyle=e.type==='shield'?'#8DB9C7':e.type==='twin'?'#E9785E':'#8BC6AD';ctx.lineWidth=4;ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#fff';ctx.fillText(e.type==='shield'?'◌':e.type==='twin'?'Ⅱ':'✦',e.x-5,e.y+5)}else{ctx.fillStyle=e.type==='comet'?'#7d6f66':e.type==='scout'?'#cc859f':e.type==='drifter'?'#a89bcb':'#9b8a77';ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#fff5';ctx.stroke()}} for(const impact of s.impacts){const progress=Math.min(1,(s.time-impact.t)/IMPACT_DURATION); drawImpact(ctx,loadout.hitEffect,impact.x,impact.y,progress)} const p=s.player; if(p.shield>0){ctx.strokeStyle='#8DB9C7';ctx.lineWidth=3;ctx.beginPath();ctx.arc(p.x,p.y,30,0,Math.PI*2);ctx.stroke()} if(p.inv>0&&!reduced)ctx.globalAlpha=.55+.35*Math.sin(s.time*24); drawEngineTrail(ctx,loadout.engineTrail,p.x,p.y,s.time,accent); drawHull(ctx,loadout.hull,p.x,p.y,accent); drawWings(ctx,loadout.wings,p.x,p.y,accent,s.time); drawCabin(ctx,loadout.cabin,p.x,p.y);ctx.globalAlpha=1}
