import { describe, expect, it } from 'vitest'
import { loadFamilyFleetRecords, saveFamilyFleetBestScore } from './records'
import { recordFamilyFleetRun } from './runStats'
function mem(): Storage { const m=new Map<string,string>(); return {getItem:k=>m.get(k)??null,setItem:(k,v)=>{m.set(k,v)},removeItem:k=>{m.delete(k)},clear:()=>m.clear(),key:i=>[...m.keys()][i]??null,get length(){return m.size}} }
describe('family fleet storage',()=>{it('recovers empty and corrupt state',()=>{const s=mem(); expect(loadFamilyFleetRecords('f',s)).toEqual({}); s.setItem('rodinka.family-fleet.records.v1.f','x'); expect(loadFamilyFleetRecords('f',s)).toEqual({})}); it('does not overwrite a higher best',()=>{const s=mem(); saveFamilyFleetBestScore('f','a',100,s); saveFamilyFleetBestScore('f','a',50,s); expect(loadFamilyFleetRecords('f',s).a).toBe(100)}); it('deduplicates run ids',()=>{const s=mem(); recordFamilyFleetRun('f','a','r1',{score:10,stars:1,targetsDestroyed:2,survivedMs:1000,highestLevel:1,powerupsCollected:0},s); const out=recordFamilyFleetRun('f','a','r1',{score:10,stars:1,targetsDestroyed:2,survivedMs:1000,highestLevel:1,powerupsCollected:0},s); expect(out.stats.a.runsPlayed).toBe(1)})})
